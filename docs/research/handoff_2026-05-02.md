# Handoff 2026-05-02

本文档用于新会话快速接手当前项目，继续完成 AI4S 电价预测与储能策略优化任务。

## 1. 当前需求

用户目标：

1. 详细理解赛题、评分规则和官方数据。
2. 基于 `D:\github\ai4s` 仓库持续优化提交文件 `output.csv`。
3. 尽量提升线上分数，同时保留可复盘、可答辩的实验记录。
4. 每次关键决策都要用通俗语言解释原因、做了什么、下一步怎么做。

赛题核心：

- 预测 2026-01-01 至 2026-02-28 共 59 天、每天 96 个 15 分钟点的电价。
- 提交 `output.csv`，其中 `power` 直接参与评分。
- 每天最多一次完整充放电：
  - 充电连续 8 点，功率 `-1000`；
  - 放电连续 8 点，功率 `+1000`；
  - 必须先充电再放电；
  - 可选择整天不交易。
- 最终分数是测试期平均日收益。

## 2. 当前线上反馈

已知线上结果：

| 文件 | 线上分数 | 结论 |
|---|---:|---|
| `outputs/output_nwp_unconstrained_online5117.csv` | `5117.832037755039` | 当前保底最高分，必须保留。 |
| `outputs/output_nwp_c0_55_d72_88.csv` | `3798.629342284567` | 明显过拟合，不再提交。 |
| `outputs/output_blend_fine_w025_t1000.csv` | `4703.505815153465` | 不再提交。 |
| `outputs/output_nwp_unconstrained_t2000.csv` | `4903.504068225546` | 不再提交。 |

当前 `output.csv` 已恢复为线上 5117 保底文件：

```text
output.csv
outputs/output_nwp_unconstrained_online5117.csv
```

两者 hash 一致：

```text
AD83C1BE3298381D39CC0848ACBE4E664A8E0860E9333D75BE7073C64D6D0AF8
```

当前小幅冲分候选：

```text
outputs/output_safe5117_skip_t500.csv
```

它只在 5117 保底文件基础上跳过 1 天：`2026-01-11`，其余 58 天动作完全不变。hash：

```text
76180A22481C3718CF7E31C068EE94C092ADE1A79038AF6EB81CBF56FCED7218
```

## 3. 当前工作进度

### 3.1 已完成的工程改造

已完成：

- `README.md` 更新为当前真实状态。
- `scripts/run_pipeline.ps1` 默认不再用本地验证最优候选覆盖 `output.csv`，而是优先恢复线上保底文件。
- 新增 `src/check_submission.py`：检查提交格式和充放电合法性。
- 增强 `src/validate_profit.py`：
  - `oracle_profit`
  - `capture_ratio`
  - `regret`
  - `window_hit`
  - `window_hit_2`
- 新增 `src/nwp_diagnostics.py`：诊断 NWP 维度和 GHI 时间对齐。
- 修改 `src/nwp_features.py`：自适应识别 `.nc` 的 hour/channel 维度。
- 新增 `src/price_history_features.py`：构造无泄漏历史价格统计特征。
- 新增 `src/select_best_submission.py`：根据本地比较表自动选择提交候选。
- 新增 `src/tune_strategy_windows.py`：窗口边界和阈值细搜。
- 新增 `src/tune_robust_strategy.py`：多 seed 不确定性惩罚调参。
- 新增 `src/train_window_ranker.py`：直接窗口收益模型。
- 新增 `src/analyze_submission_diff.py`：逐日比较两个提交文件的充放电动作差异。

### 3.2 已完成的实验

已完成：

1. NWP 诊断：
   - 当前 `.nc` 样本实际维度类似 `1 x 24 x 7 x 104 x 225`；
   - 自动识别为 `time, hour, channel, lat, lon`；
   - GHI 峰值大约在北京时间 `13:00`，夜间 GHI 为 0，暂未发现 8 小时时区错位。

2. 完整 pipeline：
   - 曾自动选择 `nwp_c0_55_d72_88`；
   - 本地验证高，但线上只有 `3798.6293`，已判定过拟合。

3. 滚动验证：
   - `c0_55_d72_88` 在滚动窗口本地仍领先；
   - 但线上反馈证明该本地验证不能可靠代表 2026 测试期。

4. residual LightGBM：
   - 本地验证 `avg_profit=9906.3860`；
   - 低于主线验证，不提交。

5. 窗口收益模型：
   - 本地验证 `avg_profit=7712.8071`；
   - 未跑赢主线，不提交。

6. robust 不确定性惩罚：
   - `lambda=0` 最优；
   - 强行惩罚 seed 分歧没有提升。

7. 保底文件动作差异分析：
   - `outputs/output_nwp_c0_55_d72_88.csv` 与 5117 保底相比，59 天没有一天动作完全相同；
   - 平均充电起点偏移约 7.37 个 15 分钟格；
   - 平均放电起点偏移约 7.34 个 15 分钟格；
   - 说明低分文件不是小优化失败，而是策略整体偏离保底。

## 4. 当前主要问题

### 问题 1：本地验证和线上测试期分布不一致

2025 年 1-2 月验证和 2026 年 1-2 月测试并不完全一致。本地最优窗口 `c0_55_d72_88` 线上大幅掉分，说明不能继续盲信本地验证排序。

### 问题 2：大幅改变充放电窗口风险极高

线上 5117 保底文件的动作与低分文件每天都不同。后续优化应优先做“保底文件基础上的小扰动”，不要直接换整套策略。

### 问题 3：当前最有价值的信息是线上反馈，而不是 RMSE

评分只看 `power` 带来的收益。后续所有候选都应先问：

```text
它相对 5117 保底文件改了哪些天？
改动幅度多大？
是否值得占用一次线上提交？
```

### 问题 4：部分旧文档曾出现编码显示问题

部分早期中文文档在终端中显示为乱码，但新写入的 `README.md`、`candidate_status.md`、`online_feedback_2026-05-02.md`、本 handoff 文档是正常 UTF-8。

## 5. 当前文件状态

当前关键文件：

```text
output.csv
outputs/output_nwp_unconstrained_online5117.csv
outputs/output_safe5117_skip_t500.csv
outputs/output_safe5117_skip_t1000.csv
outputs/candidate_similarity_to_safe5117.csv
outputs/action_diff_safe5117_vs_bad3798.csv
outputs/action_diff_safe5117_vs_bad3798_summary.csv
docs/research/candidate_status.md
docs/research/online_feedback_2026-05-02.md
```

当前 git 状态中仍有未提交改动：

```text
M docs/research/candidate_status.md
?? README.md
?? docs/research/online_feedback_2026-05-02.md
?? src/analyze_submission_diff.py
```

注意：`README.md` 显示为未跟踪，说明原仓库里可能没有被 git 跟踪，或之前被删除重建。不要误删。

## 6. 下一步工作建议

### P0：马上做

1. 如果用户只是要保底，提交当前 `output.csv`。
2. 如果用户还有提交次数且愿意小风险试探，提交：

```text
outputs/output_safe5117_skip_t500.csv
```

它只跳过 `2026-01-11` 一天，是当前最小风险冲分候选。

3. 如果 `t500` 线上高于 5117，再考虑：

```text
outputs/output_safe5117_skip_t1000.csv
```

4. 如果 `t500` 线上低于 5117，不要继续提高 threshold，说明保底文件中的低预测收益日也可能有正收益。

### P1：继续分析

1. 对 `output_safe5117_skip_t500`、`t1000`、`t1500` 做更详细的每日改动报告。
2. 分析保底文件中预测收益最低的日期：
   - `2026-01-11`
   - `2026-01-08`
   - `2026-02-20`
   - `2026-02-19`
   - `2026-01-09`
3. 尝试只改 1-3 天的候选，不要改 10 天以上。

### P2：谨慎冲榜

1. 不要再提交 `c0_55_d72_88`、residual、window ranker。
2. 不要基于单一 2025 验证收益自动选择最终提交。
3. 如果要做新模型，先把候选输出和 5117 保底做 action diff，只允许小幅偏离。

### P3：答辩材料

答辩重点可以讲：

1. 项目不只预测电价，还构建了收益回测和 oracle 诊断。
2. NWP 维度和时区做了诊断。
3. 做了多 seed、历史价格统计、窗口搜索、residual、window ranker 等实验。
4. 最终通过线上反馈发现本地过拟合，建立了“保底文件 + 小扰动候选”的稳健提交策略。

## 7. 新会话启动 Prompt

把下面这段复制到新会话即可：

```text
你现在接手 D:\github\ai4s 项目。请先阅读以下文件，快速了解当前状态：

1. D:\github\ai4s\docs\research\handoff_2026-05-02.md
2. D:\github\ai4s\docs\research\candidate_status.md
3. D:\github\ai4s\docs\research\online_feedback_2026-05-02.md
4. D:\github\ai4s\README.md

当前目标：继续优化 AI4S 电价预测与储能策略比赛提交文件。评分只看 output.csv 里的 power 带来的平均日收益。

已知线上分数：
- outputs/output_nwp_unconstrained_online5117.csv：5117.832037755039，是当前保底最高分。
- outputs/output_nwp_c0_55_d72_88.csv：3798.629342284567，已证明过拟合，禁止再提交。
- outputs/output_blend_fine_w025_t1000.csv：4703.505815153465，不再提交。
- outputs/output_nwp_unconstrained_t2000.csv：4903.504068225546，不再提交。

当前 output.csv 已恢复为 5117 保底文件，hash：
AD83C1BE3298381D39CC0848ACBE4E664A8E0860E9333D75BE7073C64D6D0AF8

当前最小风险冲分候选是：
D:\github\ai4s\outputs\output_safe5117_skip_t500.csv
它只比保底文件少交易 2026-01-11 一天，其余 58 天完全不变。提交前请先运行：
python -m src.check_submission --submission <文件>

请继续完成任务时遵守：
1. 不要再让本地验证最高候选自动覆盖 output.csv。
2. 新候选必须先和 outputs/output_nwp_unconstrained_online5117.csv 做逐日 action diff。
3. 优先做保底文件基础上的小扰动，而不是重新换整套模型。
4. 每次关键决定都用通俗语言告诉我为什么、做了什么、下一步做什么。

请先确认当前 git 状态、output.csv hash、候选文件合法性，然后规划下一步。
```

## 8. 当前会话 ID

当前 Codex 线程 ID：

```text
019dd820-df54-7792-b93b-b1366c707b06
```
